/* pages/profile/profile.wxss */

/* 底部仅 tab-bar-spacer，避免与 padding-bottom 叠加双倍留白 */
.container { background: var(--app-bg); min-height: 100vh; padding-bottom: 0; }

/* ── 用户信息卡 ─────────────────────────────── */
.user-card {
  position: relative;
  padding: 72rpx 40rpx 56rpx;
  display: flex;
  align-items: center;
  gap: 32rpx;
  border-bottom: none;
  overflow: hidden;
  background: linear-gradient(135deg, rgba(34,118,77,0.30) 0%, rgba(56,161,105,0.16) 55%, rgba(244,251,246,0.06) 100%);
}

.user-card-bg {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  opacity: 0.3;
}

.avatar-wrap {
  display: flex; flex-direction: column; align-items: center; gap: 12rpx;
  position: relative;
  z-index: 2;
}
.avatar-circle {
  width: 136rpx; height: 136rpx;
  border-radius: 50%;
  background: rgba(255,255,255,0.15);
  border: 3rpx solid rgba(255,255,255,0.35);
  display: flex; align-items: center; justify-content: center;
  box-shadow: 0 8rpx 32rpx rgba(0,0,0,0.12);
  overflow: hidden;
}
.avatar-img {
  width: 100%; height: 100%;
  border-radius: 50%;
}
.avatar-emoji { font-size: 68rpx; }
.avatar-edit-hint {
  position: absolute;
  bottom: 28rpx; right: -6rpx;
  width: 44rpx; height: 44rpx;
  background: rgba(255,255,255,0.9);
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  box-shadow: 0 4rpx 12rpx rgba(0,0,0,0.15);
  z-index: 3;
}
.edit-camera { font-size: 24rpx; }
.badge-role {
  font-size: 22rpx; padding: 8rpx 20rpx;
  border-radius: 40rpx; font-weight: 700;
  margin-top: 8rpx;
  position: relative;
  z-index: 2;
}
.badge-family {
  background: rgba(255,255,255,0.72);
  color: #0d2b1f;
  border: 1rpx solid rgba(47,133,90,0.18);
}
.badge-elderly {
  background: rgba(255,255,255,0.72);
  color: #0d2b1f;
  border: 1rpx solid rgba(47,133,90,0.18);
}
.user-info {
  flex: 1;
  position: relative;
  z-index: 2;
}
.name-row {
  display: flex; align-items: center; gap: 12rpx;
}
.user-name  {
  font-size: 44rpx;
  font-weight: 900;
  color: #0d2b1f;
  text-shadow: 0 2rpx 10rpx rgba(255,255,255,0.55);
}
.name-edit-icon { font-size: 28rpx; opacity: 0.7; color: rgba(13,43,31,0.70); }
.phone-row {
  display: flex; align-items: center; gap: 8rpx; margin-top: 14rpx;
}
.phone-icon { font-size: 24rpx; }
.user-phone { font-size: 26rpx; color: rgba(13,43,31,0.72); font-weight: 600; }

.user-card::after {
  content: '';
  position: absolute;
  top: -40%;
  right: -20%;
  width: 360rpx;
  height: 360rpx;
  border-radius: 50%;
  background: rgba(255,255,255,0.06);
  z-index: 1;
}

/* ── 统计行 ──────────────────────────────────── */
.stat-row {
  display: flex;
  background: #ffffff;
  margin: 0 36rpx;
  border-radius: var(--radius);
  padding: 36rpx 0;
  box-shadow: var(--shadow-card);
  /* 避免压住上方背景卡：不做负 margin 叠层 */
  margin-top: 50rpx;
}
.stat-item  { flex: 1; display: flex; flex-direction: column; align-items: center; }
.stat-num   { font-size: 48rpx; font-weight: 800; color: #1a6b42; }
.stat-label { font-size: 26rpx; color: #4a6355; margin-top: 8rpx; }
.stat-divider { width: 1rpx; background: rgba(47,133,90,0.12); }

/* ── 分区标题 ────────────────────────────────── */
.section-title {
  font-size: 26rpx; color: #6b8577;
  letter-spacing: 3rpx;
  font-weight: 700;
  padding: 40rpx 40rpx 18rpx;
  text-transform: uppercase;
}

/* ── 守护对象卡片 ────────────────────────────── */
.elderly-card { margin: 0 36rpx 0; }
.elderly-row  { display: flex; align-items: center; gap: 24rpx; margin-bottom: 18rpx; }
.elderly-avatar { font-size: 64rpx; }
.elderly-info { flex: 1; }
.elderly-name { display: block; font-size: 36rpx; font-weight: 700; color: #0d2b1f; }
.elderly-meta { display: block; font-size: 26rpx; color: #4a6355; margin-top: 6rpx; }
.status-dot   { width: 20rpx; height: 20rpx; border-radius: 50%; }
.status-dot.safe      { background: var(--green); box-shadow: 0 0 0 6rpx var(--green-pale); }
.status-dot.warning   { background: var(--amber); box-shadow: 0 0 0 6rpx var(--amber-pale); }
.status-dot.emergency { background: var(--red);   box-shadow: 0 0 0 6rpx var(--red-pale);   }
.elderly-location { display: flex; align-items: center; gap: 10rpx; }
.location-icon { font-size: 28rpx; }
.location-text { font-size: 28rpx; color: #4a6355; }

/* ── 绑定状态圆点 ────────────────────────────────────── */
.bind-status-dot {
  width: 16rpx; height: 16rpx;
  border-radius: 50%;
  flex-shrink: 0;
  margin-right: 8rpx;
}
.dot-bound   { background: var(--green); box-shadow: 0 0 0 4rpx var(--green-pale); }
.dot-unbound { background: var(--amber); box-shadow: 0 0 0 4rpx var(--amber-pale); }

/* ── 菜单组 ──────────────────────────────────── */
.menu-group {
  margin: 0 36rpx 0;
  background: #ffffff;
  border: 1rpx solid rgba(47,133,90,0.08);
  border-radius: var(--radius);
  overflow: hidden;
  box-shadow: var(--shadow-card);
}
.menu-row {
  display: flex;
  align-items: center;
  padding: 34rpx 32rpx;
  border-bottom: 1rpx solid rgba(47,133,90,0.06);
}
.menu-row:last-child { border-bottom: none; }
.menu-icon  { font-size: 44rpx; margin-right: 24rpx; flex-shrink: 0; }
.menu-main  { flex: 1; }
.menu-label { display: block; font-size: 32rpx; color: #0d2b1f; font-weight: 600; }
.menu-label-red { flex: 1; font-size: 32rpx; color: var(--red); font-weight: 700; }
.menu-sub   { display: block; font-size: 26rpx; color: #4a6355; margin-top: 6rpx; }
.menu-arrow { font-size: 40rpx; color: #8a9e92; }
.menu-badge {
  background: var(--red); border-radius: 20rpx;
  min-width: 36rpx; height: 36rpx;
  display: flex; align-items: center; justify-content: center;
  padding: 0 8rpx; margin-right: 12rpx;
}
.menu-badge-text { font-size: 20rpx; color: #fff; font-weight: 700; }

/* ── 版本号 ──────────────────────────────────── */
.version-tip {
  text-align: center;
  font-size: 24rpx;
  color: #6b8577;
  padding: 48rpx 0 16rpx;
}

/* ══════ 老人端专属样式 ══════ */
.e-user-card {
  position: relative;
  padding: 72rpx 40rpx 56rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 18rpx;
  border-bottom: none;
  overflow: hidden;
  background: linear-gradient(135deg, rgba(34,118,77,0.28) 0%, rgba(56,161,105,0.15) 55%, rgba(244,251,246,0.06) 100%);
}

.e-user-card-bg {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  opacity: 0.3;
}

.e-avatar-wrap {
  position: relative;
  display: flex; align-items: center; justify-content: center;
  z-index: 2;
}
.e-avatar-circle {
  width: 168rpx; height: 168rpx;
  border-radius: 50%;
  background: rgba(255,255,255,0.18);
  border: 4rpx solid rgba(255,255,255,0.4);
  display: flex; align-items: center; justify-content: center;
  overflow: hidden;
}
.e-avatar-img {
  width: 100%; height: 100%;
  border-radius: 50%;
}
.e-avatar-emoji { font-size: 92rpx; }
.e-avatar-edit-hint {
  position: absolute;
  bottom: 0; right: -8rpx;
  width: 52rpx; height: 52rpx;
  background: rgba(255,255,255,0.9);
  border-radius: 50%;
  display: flex; align-items: center; justify-content: center;
  box-shadow: 0 4rpx 12rpx rgba(0,0,0,0.15);
  z-index: 3;
}
.e-name-row {
  display: flex; align-items: center; gap: 16rpx;
  position: relative;
  z-index: 2;
}
.e-user-name    {
  font-size: 56rpx;
  font-weight: 900;
  color: #0d2b1f;
  text-shadow: 0 2rpx 10rpx rgba(255,255,255,0.55);
}
.e-name-edit-icon { font-size: 32rpx; opacity: 0.7; color: rgba(13,43,31,0.70); }
.e-phone-row {
  display: flex; align-items: center; gap: 10rpx;
  position: relative;
  z-index: 2;
}
.e-phone-icon { font-size: 28rpx; }
.e-user-phone { font-size: 30rpx; color: rgba(13,43,31,0.72); font-weight: 600; }
.e-user-sub     { font-size: 28rpx; color: rgba(13,43,31,0.66); font-weight: 600; }

.e-user-card .badge-role {
  position: relative;
  z-index: 2;
}

.e-user-card::after {
  content: '';
  position: absolute;
  top: -30%;
  left: -20%;
  width: 400rpx;
  height: 400rpx;
  border-radius: 50%;
  background: rgba(255,255,255,0.05);
  z-index: 1;
}

/* 分区标题（老人端大字） */
.e-section-title {
  font-size: 32rpx; font-weight: 800;
  color: #2a4d3a;
  padding: 36rpx 40rpx 16rpx;
  letter-spacing: 2rpx;
}

/* 联系人卡片 */
.e-contact-card {
  display: flex;
  align-items: center;
  background: #ffffff;
  border: 1rpx solid rgba(47,133,90,0.08);
  border-radius: var(--radius);
  margin: 0 36rpx 20rpx;
  padding: 32rpx;
  gap: 24rpx;
  box-shadow: var(--shadow-card);
}
.e-contact-avatar { font-size: 64rpx; }
.e-contact-info   { flex: 1; }
.e-contact-name   { display: block; font-size: 38rpx; font-weight: 700; color: #0d2b1f; }
.e-contact-relation { display: block; font-size: 28rpx; color: #4a6355; margin-top: 6rpx; }
.e-call-btn {
  display: flex; flex-direction: column; align-items: center;
  background: var(--green-pale);
  border: 1rpx solid rgba(93,217,127,0.3);
  border-radius: var(--radius-sm);
  padding: 16rpx 24rpx;
  gap: 6rpx;
}
.e-call-icon { font-size: 40rpx; }
.e-call-text { font-size: 22rpx; color: var(--green); font-weight: 600; }
.e-call-btn-amber {
  background: var(--amber-pale);
  border-color: rgba(245,166,35,0.3);
}
.e-call-text-amber {
  color: var(--amber);
}
.e-contact-card-tight { margin-bottom: 0; }
.e-empty-tip { text-align: center; color: var(--text-dim); font-size: 28rpx; padding: 24rpx; }

/* 今日提醒 */
.e-reminder-card {
  margin: 0 32rpx 0;
  background: var(--glass);
  border: 1rpx solid var(--glass-border);
  border-radius: var(--radius);
  overflow: hidden;
}
.e-reminder-row {
  display: flex; align-items: center; gap: 20rpx;
  padding: 28rpx;
  border-bottom: 1rpx solid var(--glass-border);
}
.e-reminder-row:last-child { border-bottom: none; }
.e-reminder-icon  { font-size: 44rpx; }
.e-reminder-info  { flex: 1; }
.e-reminder-title { display: block; font-size: 30rpx; font-weight: 600; color: var(--text); }
.e-reminder-time  { display: block; font-size: 24rpx; color: var(--text-dim); margin-top: 4rpx; }
.e-reminder-done    { font-size: 22rpx; color: var(--green); font-weight: 600; padding: 8rpx 16rpx; background: var(--green-pale); border-radius: 40rpx; }
.e-reminder-pending { font-size: 22rpx; color: var(--amber); font-weight: 600; padding: 8rpx 16rpx; background: var(--amber-pale); border-radius: 40rpx; }

/* 功能四宫格 */
.e-func-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 20rpx;
  margin: 0 32rpx;
}
.e-func-item {
  display: flex; flex-direction: column; align-items: center;
  background: var(--glass);
  border: 1rpx solid var(--glass-border);
  border-radius: var(--radius);
  padding: 36rpx 20rpx;
  gap: 16rpx;
}
.e-func-icon { font-size: 56rpx; }
.e-func-text { font-size: 28rpx; color: var(--text); font-weight: 600; }

/* 退出按钮 */
.e-logout-btn {
  margin: 40rpx 36rpx 0;
  background: rgba(239,68,68,0.06);
  border: 1rpx solid rgba(239,68,68,0.15);
  border-radius: var(--radius);
  padding: 32rpx;
  text-align: center;
}
.e-logout-text { font-size: 30rpx; font-weight: 600; color: var(--red); }
.e-cancel-btn {
  margin-top: 24rpx;
  background: rgba(255,92,92,0.08);
}
